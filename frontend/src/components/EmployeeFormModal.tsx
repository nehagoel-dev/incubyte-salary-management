import type { Employee } from '../lib/api'

interface Props {
  opened: boolean
  onClose: () => void
  employee?: Employee
}

export default function EmployeeFormModal(_props: Props) {
  return null
}
